extraction:
  java:
    before_index:
      export BUILD_MODE=release
    index:
      maven:
        version: 3.8.4
        settings_file: pom.xml 
      build_command:
        - mvn clean spring-boot:run
